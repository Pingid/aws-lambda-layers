import type { Serverless } from "serverless/aws";
declare const _default: ({ name, region, stage, }: {
    region: string;
    stage: string;
    name: string;
}) => Serverless;
export default _default;
